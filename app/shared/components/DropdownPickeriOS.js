import React from 'react';
import {
  ActionSheetIOS,
  StyleSheet,
} from 'react-native';

// Cannot use the same module as Android.
// Use ActionSheetiOS instead.
const DropdownPickeriOS = () => {
  return (
    null
  );
}

const styles = () => StyleSheet.create({

});
  
export default DropdownPickeriOS;